# null이 아닌, 빈 컬렉션이나 배열을 반환하라
- 요약
  - null을 반환하는 API는 사용하기 어렵고 오류 처리 코드도 늘어난다. 그렇다고 성능이 좋은 것도 아니다.
  - 빈 컨테이너를 할당하는데도 비용이 드니 null을 반환하자는 주장도 있는데, 이는 틀린 주장이다.
    - 성능 분석 결과 큰 차이가 없는 것으로 판명났다.
    - 빈 컬렉션이나 배열은 굳이 새로 할당하지 않고도 반환할 수 있다.
- 질문